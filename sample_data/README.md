# Sample Data
Run `python scripts/generate_fake_inputs.py` to create deterministic fake data under `sample_data/generated/`.

Includes fake .txt, .pdf, .docx, audio (.mp3 when ffmpeg available, else .wav), video (.webm placeholder), captions (.srt/.vtt), metadata JSON, and retrieval manifests.

This data is synthetic and safe for local testing only.
